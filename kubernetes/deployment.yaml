apiVersion: apps/v1
kind: Deployment
metadata:
  name: tigergate-test-c
spec:
  replicas: 1
  selector:
    matchLabels: { app: tigergate-test-c }
  template:
    metadata:
      labels: { app: tigergate-test-c }
    spec:
      hostNetwork: true # Deliberately insecure IaC fixture.
      containers:
        - name: app
          image: tigergate-test-c:latest
          securityContext:
            privileged: true
            runAsUser: 0
          env:
            - name: DATABASE_PASSWORD
              value: "fixture-password-not-real"
